version: 2

models:
  - name: int_local_bike__products
    description: "Table who is the fusion of products, categories and brands of staging."
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each product."
        tests:
            - unique
            - not_null